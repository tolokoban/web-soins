<?php
$ROLE = "";

include_once("./data.php");


/*
   Return code:
   -1: Missing argument `name`.
 */
function execService( $args ) {
    if( !array_key_exists( 'name', $args ) ) return -1;
    $name = $args['name'];
    return \Data\Structure\add([ 'name' => $name ]);
}
?>
